# 参与贡献

## 开发要求

- Node.js 20 或更高版本。
- 不引入运行时依赖，除非标准库无法可靠完成需求。
- 不提交 `.env`、API Key、真实接口响应或用户生成图片。
- 保持 `SKILL.md` 简洁，把用户安装与维护说明留在 README。

## 修改流程

1. 从仓库创建分支。
2. 修改脚本或 Skill 指令。
3. 运行 `npm test`。
4. 使用 Skill Creator 的 `quick_validate.py` 验证 `SKILL.md`。
5. 提交清晰的变更说明和脱敏后的验证结果。

## 行为要求

- 新增尺寸策略时，补充对应测试。
- 新增接口字段时，默认保持第三方网关兼容性。
- 修改编辑接口时，同时覆盖单图、多图和蒙版的 multipart 测试。
- 不对 `429`、超时或未知错误进行隐式付费重试。
- 错误输出必须对 API Key 脱敏。
- 保存图片前验证实际文件格式；不要只相信 URL 后缀或响应头。
