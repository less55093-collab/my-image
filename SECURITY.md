# 安全说明

## API Key

- 不要把真实 API Key 写进 `SKILL.md`、README、Issue、截图或提交记录。
- 优先使用首次调用时自动打开的本地配置页。
- 如果 Key 曾经出现在聊天、日志、终端历史或公开仓库中，应立即在服务商控制台撤销并重新生成。
- 配置文件位于用户配置目录，不位于 Skill 仓库内。

## 本地配置页

- 配置服务只监听 `127.0.0.1`，不会监听局域网或公网地址。
- 每次启动会生成随机令牌，并通过带令牌的临时 URL 提交配置。
- 页面禁用缓存、外部资源、跨站框架和不必要的网络连接。
- 配置完成或等待超时后，本地服务会退出。

## 本地存储

- macOS/Linux 会尝试把 `.env` 权限设为 `0600`。
- Windows 会尝试移除继承 ACL，并仅向当前用户授予访问权限。
- API Key 仍以明文形式保存在当前用户目录。磁盘加密、系统账户安全和恶意软件防护仍由用户负责。

## 图片与提示词隐私

- 生成提示词、编辑原图、参考图和蒙版会发送到用户配置的第三方 Base URL。
- 不要向不可信服务上传身份证件、商业机密、未公开设计稿或其他敏感图片。
- 使用前应确认接口服务商的数据保留、训练使用、日志记录和删除政策。
- 本 Skill 不会把用户图片上传到仓库，但第三方接口的处理行为不受本仓库控制。

## 报告问题

安全问题不要附带真实 API Key、完整请求头或未脱敏的配置文件。请通过 Gitee 私信联系仓库所有者，或提交不含敏感信息的最小复现说明。

报告中可以包含：

- 受影响的脚本和版本或提交号。
- 操作系统与 Node.js 版本。
- 已脱敏的错误信息。
- 可使用假 Key 复现的步骤。
